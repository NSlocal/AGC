<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <application
        android:name=".SafetyCoreApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafetyCore"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SafetyCore.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.SafetyCore" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.SafetyCore" />

        <activity
            android:name=".ui.FloatingSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.SafetyCore" />

        <activity
            android:name=".ui.BenchmarkActivity"
            android:exported="false"
            android:theme="@style/Theme.SafetyCore" />

        <activity
            android:name=".ui.GameListActivity"
            android:exported="false"
            android:theme="@style/Theme.SafetyCore" />

        <activity
            android:name=".ui.ThemePickerActivity"
            android:exported="false"
            android:theme="@style/Theme.SafetyCore" />

        <activity
            android:name=".ui.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.SafetyCore" />

        <!-- FPS Overlay Service -->
        <service
            android:name=".overlay.FPSOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="fps_monitor" />
        </service>

        <!-- Game Boost Service -->
        <service
            android:name=".service.GameBoostService"
            android:exported="false" />

        <!-- Quick Settings Tile Service -->
        <service
            android:name=".ui.QuickSettingsTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_menu_info_details"
            android:label="FPS Monitor"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Broadcast Receiver -->
        <receiver
            android:name=".receiver.PackageReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
